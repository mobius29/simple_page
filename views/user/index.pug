extends ../layout.pug

block title 
	| Users 

block page_title
	| All Users 

block content
	each user in list 
		ul 
			li
				a(href=`/user/user?displayName=${user.displayName}`) #{user.displayName}